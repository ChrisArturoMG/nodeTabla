#Notas 
```
    Options:
        --help     Show help                                             [boolean]
        --version  Show version number                                   [boolean]
    -b, --base     Es la base de la tabla de multiplicar       [number] [required]
    -l, --listar   Muestra los cambios en consola       [boolean] [default: false]
        --limite   Indica el limite hasta donde calcular la tabla         [number]
```